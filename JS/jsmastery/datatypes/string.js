// creating a string in javascript

let studentName = "Harry";
console.log(studentName + " is a " + typeof studentName)

  //string

  // in javascript there are three types of strings


  const singleQuotes = 'Hello!';
  const doubloeQuotes = "Hello!!";
  const name = 'jane';
  const backticks = `Hello, ${name}`;
  const back = `Hello, ${2+2}`;
  console.log(singleQuotes);
  console.log(doubloeQuotes);
  console.log(name); 
  console.log(backticks);
  console.log(back);

  // there are called simple strings, and they are identically same