// cookies in javascript

// cookies are key values pairs

// they are small strings of data stored directally in the browser

// cookies are set by a web server using the Set-Cookie, HTTP-Header, next
// time when the request is sent to the same domain, the
// browswe sends the cookie using the cookie http-loader
// that way the server knows who sent the request

// we can also cookies using document.cookies